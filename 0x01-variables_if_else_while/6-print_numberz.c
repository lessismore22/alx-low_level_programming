#include <stdio.h>
/**
 * main- this prints all single digit numbers in base 10
 * starting from 0
 *
 * Return:0 if succesful
 */

int main(void)
{
	int n;

	for (n = '0'; n < '10'; n++)
	{
		putchar(n);
	}
		putchar('\n');

		return (0);
}

