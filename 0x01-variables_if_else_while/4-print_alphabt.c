#include <stdio.h>
/**
 * main- this function prints the alphabets excluding
 * the letters 'c' and 'e'
 *
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		if (s != 'c' && s != 'e')
		{
			putchar(s);
		}


		putchar('\n');
		return (0);

	}
}
