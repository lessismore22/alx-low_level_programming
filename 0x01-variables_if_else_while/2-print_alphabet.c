#include <stdio.h>
/*
 * main - this prints the letters of the alphabet from A-Z
 * in lower case
 *
 *
*/
int main(void)
{
char letters;
	for (letters = a; letter <= z; letter++)
	{
	putchar(letter);
	putchar('\n');
	}
}
