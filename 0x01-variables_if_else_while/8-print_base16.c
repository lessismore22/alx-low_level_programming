#include <stdio.h>
/**
  * main - this function prints numbers of base 16 in lower case
  *
  *
  * Return: 0 Always succesful
 */
int main(void)
{
	int n;

	for (n = 0; n <= 15; n++)

	{
		putchar(n);
	}
	putchar('\n');

	return (0);
