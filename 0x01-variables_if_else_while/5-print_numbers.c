#include <stdio.h>
/**
 * main - this prints all single digit numbers in base 10
 * starting from 0.
 *
 *
 * Returns (0) if successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
		putchar('\n');

		return (0);
}
