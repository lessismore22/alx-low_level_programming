#include "main.h"
/**
 * _abs - this function prints the absolute value of a digit
 * @int: character type
 * Return: 0 if succesful
 *
 */
int _abs(int)
{
	int n;

	if (n > 0)
	{
		_putchar(n);
	}

	else if (n < 0)
	{
		_putchar(n);
	}

	else
	{
		_putchar(0);
	}

	return (0);
}
