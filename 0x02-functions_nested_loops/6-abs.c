#include "main.h"
/**
 * _abs - this function prints the absolute value of a digit
 * @x: character type
 * Return: 0 if succesful
 *
 */
int _abs(int x)
{
	if (x > 0)
	{
		return (x);
	}

	else if (x < 0)
	{
		return (x);
	}

	else
	{


		return (0);
}
